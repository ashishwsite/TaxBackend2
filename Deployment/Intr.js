import React from 'react'

const Intr = () => {
  return (
    <div>
       1. for Deployment  we used (render.com )and (netlify)
      2.frontend on netlify  
      3.Backend on reder.com
      4. versal is also an hosting site <br/>
      STEP TO  HOST MERN APP
      1.versal account login in with github 
      2.change route name (localhost to winow infrontend)
      5: move frontend folder to Backend folder  , then run (npm i path)
      6. add some line of code in index.js file
      7.make versal.json file

    </div>
  )
}

export default Intr
